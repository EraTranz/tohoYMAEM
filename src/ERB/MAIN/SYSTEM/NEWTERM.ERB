;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;強くてニューゲーム関連
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;強くてニューゲームの処理はここで実行
;引き継げるものは一部主人の素質(周回やエンドの条件により獲得する素質も)・最も好感度が高い助手(ただし好感度は大きく低下)・
;開発した触手の能力・蒐集品・陥落させた(恋慕or淫乱を付けた)人数・妖精を魅了(陥落させた)人数・お金()・複数取得可能なアイテムである。
;また、現在の設定や主人公の性別(体格他最初に設定できた分も含む)を引きつづあるいは変更することが可能。
;=============================================================================
;引き継ぎ処理他
;=============================================================================
@START_NEWGAME
VARSET LOCAL, 0
;初回周回でEASYからの引き継ぎ時のみ調合知識と魅惑は消滅
IF FLAG:3 == 1 && FLAG:6 == 1
	TALENT:MASTER:調合知識 = 0
	TALENT:MASTER:魅惑 = 0
ENDIF

LOADGLOBAL
;初回周回でLUNATIC/PHANTASM/ULTRA/OVERDRIVEクリアならば偉業を称えて称号贈呈
IF (FLAG:3 == 7 && FLAG:6 <= 2)
	CFLAG:MASTER:157 |= 32
	NICKNAME:MASTER = 調教の神
	;実績
	SIF !(GLOBAL:2300 & 4)
		GLOBAL:2300 |= 4
ELSEIF (FLAG:3 == 6 && FLAG:6 == 1)
	CFLAG:MASTER:157 |= 16
	NICKNAME:MASTER = 調教師を超えた存在
	;実績
	SIF !(GLOBAL:2300 & 2)
		GLOBAL:2300 |= 2
ELSEIF FLAG:3 == 5 && FLAG:6 == 1
	CFLAG:MASTER:157 |= 2
	NICKNAME:MASTER = 空前絶後の天才調教師
	;実績
	SIF !(GLOBAL:2300 & 1)
		GLOBAL:2300 |= 1
ELSEIF FLAG:3 == 4 && FLAG:6 == 1
	CFLAG:MASTER:157 |= 1
	NICKNAME:MASTER = 期待の秀才調教師
	;実績
	SIF !(GLOBAL:2300 & 1024)
		GLOBAL:2300 |= 1024
ENDIF
;実績
SIF FLAG:8669 & 2 && !(GLOBAL:2300 & 512)
	GLOBAL:2300 |= 512
SAVEGLOBAL

;日付、時間、調教対象や助手のリセット
DAY = 1
TIME = 0
CALL CALENDAR_INIT
TARGET = -1
ASSI = -1
;目標金額
SIF MONEY:100 > 1000000
	MONEY:100 = 1000000
;購入した土地の数をここで先にリセット
FLAG:93 = 0
;娼館にいる人数をリセット
FLAG:3001 = 0

;--------------------------------------------------
;特定条件を満たすと主人に新たな素質が追加される処理
;--------------------------------------------------
;条件を満たすとの中から一つ取得できる
;ただし治療は調合知識を習得した後でしか取れない
;ここでのローカル変数LOCAL:1は追加される素質番号を示す
LOCAL:1 = 0
;1回以上クリアかつ単体エンドを見た場合、舌使いを取得可能
IF FLAG:6 >= 1 && (ITEM:赤い首輪 || ITEM:エンゲージリング) && TALENT:MASTER:舌使い == 0
	LOCAL:1 = 52
;1回以上クリアかつノーマルハーレムエンドを見た場合、小悪魔を取得可能
ELSEIF FLAG:6 >= 1 && FLAG:10002 && TALENT:MASTER:小悪魔 == 0
	LOCAL:1 = 93
;3回以上クリアかつ堕とした人数がその周回で20人以上のとき、魅惑を取得可能
ELSEIF FLAG:6 >= 3 && CFLAG:MASTER:98 >= 20 && TALENT:MASTER:魅惑 == 0
	LOCAL:1 = 91
;5回以上クリアかつその周回で烙印を押した人数が9人以上のとき、針さばきを取得可能
ELSEIF FLAG:6 >= 5 && FLAG:31 >= 9 && TALENT:MASTER:針さばき == 0
	LOCAL:1 = 53
;7回以上クリアかつ蒐集品を12種全て30種類以上集めたとき、調合知識を取得可能
ELSEIF FLAG:6 >= 7 && TALENT:MASTER:調合知識 == 0
	LOCAL:2 = 0
	FOR LOCAL, 0, 12
		SIF FLAG:(501 + LOCAL) >= 30
			LOCAL:2 += 1
	NEXT
	SIF LOCAL:2 >= 12
		LOCAL:1 = 55
;9回以上クリアかつ恋慕か服従の付いた奴隷の中で調合知識または治療持ちが4人以上いたとき、治療を取得可能
ELSEIF FLAG:6 >= 9 && TALENT:MASTER:治療 == 0
	LOCAL:2 = 0
	FOR LOCAL, 0, CHARANUM
		;主人あるいは失踪中のキャラは対象外
		SIF LOCAL == MASTER || CFLAG:LOCAL:4 == 1 || CFLAG:LOCAL:4 == 8192
			CONTINUE
		;恋慕か服従を持ち、調合知識か治療を持っている奴隷をカウントする
		SIF (TALENT:LOCAL:恋慕 || TALENT:LOCAL:服従) && (TALENT:LOCAL:調合知識 || TALENT:LOCAL:治療)
			LOCAL:2 += 1
	NEXT
	SIF LOCAL:2 >= 4
		LOCAL:1 = 59
ENDIF
;つるぺた堕し、大乳導を取得していない時、通常・ハーレムENDを合計20種類以上見た、
;奴隷が全員恋慕または淫乱を持っている、かつ今回HARDモード以上でエンディングを迎えた場合、
;奴隷全員が絶壁または貧乳を持っているとつるぺた堕しを取得可能
;同様の条件で奴隷全員が巨乳または爆乳の場合は大乳導を取得可能
;※あくまでも引き継いだ時点で所有している奴隷の能力です
IF FLAG:10000 >= 20 && TALENT:MASTER:大乳導 == 0 && TALENT:MASTER:つるぺた堕し == 0 && FLAG:3 >= 3 && LOCAL:1 == 0
	LOCAL:2 = 0
	LOCAL:3 = 0
	FOR LOCAL, 0, CHARANUM
		;主人あるいは失踪中のキャラは対象外
		SIF LOCAL == MASTER || CFLAG:LOCAL:4 == 1 || CFLAG:LOCAL:4 == 8192
			CONTINUE
		;恋慕も淫乱も持っていないなら対象外です
		SIF TALENT:LOCAL:恋慕 == 0 && TALENT:LOCAL:淫乱 == 0
			CONTINUE
		;でかいおっぱいっていいよね！
		SIF TALENT:LOCAL:巨乳 || TALENT:LOCAL:爆乳
			LOCAL:2 += 1
		;ちっぱい！こそ正義！
		SIF TALENT:LOCAL:絶壁 || TALENT:LOCAL:貧乳
			LOCAL:3 += 1
	NEXT
	SIF LOCAL:2 == CHARANUM - 1
		LOCAL:1 = 96
	SIF LOCAL:3 == CHARANUM - 1
		LOCAL:1 = 97
ENDIF

SIF LOCAL:1 > 0 && TALENT:MASTER:(LOCAL:1) == 0
	CALL NEWTERM_TALENT_MASTER, LOCAL:1
;地獄の勲章(別名：へる子購入する権利)
SIF (TALENT:MASTER:野菜の錬金術師 == 0 || FLAG:9888) && TALENT:MASTER:地獄の勲章 == 0 && ((CFLAG:MASTER:157 & 2 && FLAG:6 == 1) || ((FLAG:3 == 6 || FLAG:3 == 7) &&  FLAG:6 >= 1 && FLAG:6 <= 2))
	CALL NEWTERM_TALENT_MASTER, 834

;--------------------------------------------------
;主人の能力の引き継ぎ
;--------------------------------------------------
;経験は初期化される
LOCAL:1 = NO:MASTER
ADDCHARA LOCAL:1
TARGET = CHARANUM-1

;能力はそのまま保存
FOR LOCAL, 0, 100
	ABL:LOCAL = ABL:MASTER:LOCAL
NEXT

;種族変更フラグ
CFLAG:5 = CFLAG:MASTER:5
;IDもコピー
CFLAG:3109 = CFLAG:MASTER:3109
;種族遺伝子および性別反転関連のCFLAGをコピー
FOR LOCAL, 3200, 3400
	CFLAG:LOCAL = CFLAG:MASTER:LOCAL
NEXT
FOR LOCAL, 4995, 5000
	CFLAG:LOCAL = CFLAG:MASTER:LOCAL
NEXT

;触手エネルギー
BASE:触手 = BASE:MASTER:触手
;名前は保存
NAME:TARGET = %NAME:MASTER%
CALLNAME:TARGET = %CALLNAME:MASTER%
NICKNAME:TARGET = %NICKNAME:MASTER%

;素質保存
;購入できる素質の内、[サド][マゾ][早漏][遅漏]はリセット
;蓬莱人
	TALENT:蓬莱人 = TALENT:MASTER:蓬莱人
;舌使い
	TALENT:舌使い = TALENT:MASTER:舌使い
;針さばき
	TALENT:針さばき = TALENT:MASTER:針さばき
;調合知識
	TALENT:調合知識 = TALENT:MASTER:調合知識
;禁断の知識
	TALENT:禁断の知識 = TALENT:MASTER:禁断の知識
;妖精知識
	TALENT:妖精知識 = TALENT:MASTER:妖精知識
;淫具知識
	TALENT:淫具知識 = TALENT:MASTER:淫具知識
;治療
	TALENT:治療 = TALENT:MASTER:治療
;汚れ無視
	TALENT:汚れ無視 = TALENT:MASTER:汚れ無視
;魅惑
	TALENT:魅惑 = TALENT:MASTER:魅惑
;謎の魅力
	TALENT:謎の魅力 = TALENT:MASTER:謎の魅力
;小悪魔
	TALENT:小悪魔 = TALENT:MASTER:小悪魔
;人気
	TALENT:人気 = TALENT:MASTER:人気
;つるぺた堕し
	TALENT:大乳導 = TALENT:MASTER:大乳導
;大乳導
	TALENT:つるぺた堕し = TALENT:MASTER:つるぺた堕し
;大甲斐性
	TALENT:大甲斐性 = TALENT:MASTER:大甲斐性
;教育者
	TALENT:教育者 = TALENT:MASTER:教育者
;触手使役術
	TALENT:触手使役術 = TALENT:MASTER:触手使役術
;家電使役知識
	TALENT:家電使役知識 = TALENT:MASTER:家電使役知識
;工作名人
	TALENT:工作名人 = TALENT:MASTER:工作名人
;魔術技能
	TALENT:魔術技能 = TALENT:MASTER:魔術技能
;超常目算
	TALENT:超常目算 = TALENT:MASTER:超常目算
;種族素質
FOR LOCAL, 200, 226
	TALENT:LOCAL = TALENT:MASTER:LOCAL
NEXT
;野菜の錬金術師
	TALENT:野菜の錬金術師 = TALENT:MASTER:野菜の錬金術師
;Spell Card
	TALENT:スペルカード = TALENT:MASTER:スペルカード
;地獄の勲章
	TALENT:地獄の勲章 = TALENT:MASTER:地獄の勲章
;変遷
	TALENT:変遷 = TALENT:MASTER:変遷
;デバッグモード
	TALENT:998 = TALENT:MASTER:998

;ABNORMAL用
;逆調教の性格は対象外
IF FLAG:4 == 1
	TALENT:処女 = TALENT:MASTER:処女
	TALENT:男嫌い = TALENT:MASTER:男嫌い
	TALENT:女嫌い = TALENT:MASTER:女嫌い
	TALENT:両刀 = TALENT:MASTER:両刀
	TALENT:絶壁 = TALENT:MASTER:絶壁
	TALENT:貧乳 = TALENT:MASTER:貧乳
	TALENT:巨乳 = TALENT:MASTER:巨乳
	TALENT:爆乳 = TALENT:MASTER:爆乳
	TALENT:オトコ = TALENT:MASTER:オトコ
	TALENT:ふたなり = TALENT:MASTER:ふたなり
	TALENT:小人体型 = TALENT:MASTER:小人体型
	TALENT:小柄体型 = TALENT:MASTER:小柄体型
	TALENT:巨躯 = TALENT:MASTER:巨躯
	TALENT:理性 = TALENT:MASTER:理性
	;外見設定
	REPEAT 50
		TALENT:(560 + COUNT) = TALENT:MASTER:(560 + COUNT)
	REND
	EXP:Ｖ経験 = EXP:MASTER:Ｖ経験
	EXP:レズ経験 = EXP:MASTER:レズ経験
	EXP:ＢＬ経験 = EXP:MASTER:ＢＬ経験
ENDIF

;触手の珠
JUEL:触手 = JUEL:MASTER:触手

CALL MASTER_MAKE

SWAPCHARA MASTER, TARGET

TARGET = -1
MASTER = 0
DELCHARA CHARANUM-1

;主人の種族変更処理
SIF FLAG:70
	CALL CHANGE_RACE_MASTER

;蓬莱人引き継ぎ処理 
;あなたが人間（？）でなければ引き継ぎ処理が行われないようにしておく
IF TALENT:MASTER:蓬莱人 == 1 && CFLAG:MASTER:5 == 0
	CALL TAKEOVER_TALENT_HOURAI, 49
ELSE
	TALENT:MASTER:蓬莱人 = 0
ENDIF

;--------------------------------------------------
;助手引き継ぎ可能人数計算下準備
;--------------------------------------------------
;FLAG:8を使用。一度でも人数増加の条件を満たしていればと再度条件を満たす必要はなくなる
;[大甲斐性]を持っている
SIF TALENT:MASTER:大甲斐性 && (FLAG:8 & 1) == 0
	FLAG:8 |= 1
;難易度PAHANTASM以上を1度でもクリアしたことがある
SIF (FLAG:9 & 16 || FLAG:9 & 64 || FLAG:9 & 128) && (FLAG:8 & 2) == 0
	FLAG:8 |= 2
;調教師Lvが5になっている
SIF ABL:MASTER:調教師Lv > 4 && (FLAG:8 & 4) == 0
	FLAG:8 |= 4
;調教中に恋慕処女を頂いた人数の累計が100人以上
SIF FLAG:400 >= 100 && (FLAG:8 & 8) == 0
	FLAG:8 |= 8
;奴隷売却で稼いだ金額の累計が25,000,000圓以上、または、PROSUTITUTEで売春で稼いだ累計金額が1億以上
SIF (MONEY:990 >= 25000000 && (FLAG:8 & 16) == 0) || (MONEY:977 >= 100000000 && FLAG:4 == 4)
	FLAG:8 |= 16
;高級奴隷売却で稼いだ金額の累計が10,000,000圓以上、または、PROSUTITUTEで売春で稼いだ累計金額が1000万以上
SIF (MONEY:991 >= 10000000 && (FLAG:8 & 32) == 0) || (MONEY:977 >= 10000000 && FLAG:4 == 4)
	FLAG:8 |= 32
;奴隷売却で稼いだ最高金額が400000圓以上、または、PROSUTITUTEで1回の売春で稼いだ最高金額が10万以上
SIF (MONEY:997 >= 400000 && (FLAG:8 & 64) == 0) || (MONEY:973 >= 100000 && FLAG:4 == 4)
	FLAG:8 |= 64
;主人が妖精、吸血鬼、機械、亡霊、人形、悪魔、河童、(妖狐)、ワーハクタク、ゾンビフェアリーに転生資格がある
SIF FLAG:70 & 1 && FLAG:70 & 2 && FLAG:70 & 4 && FLAG:70 & 8 && FLAG:70 & 16 && FLAG:70 & 32 && FLAG:70 & 64 && FLAG:70 & 256 && FLAG:70 & 512 && (FLAG:8 & 128) == 0
	FLAG:8 |= 128
;開発した風呂と触手の能力の総数が45以上
SIF (FLAG:8 & 256) == 0
	CALL CHECK_BATH_AND_TENTACLE
;幻想郷秘宝館になるまで展示室を発展させた
SIF FLAG:500 >= 500 && (FLAG:8 & 512) == 0
	FLAG:8 |= 512
;ノーマル・ハーレムエンドを20種類以上見た
IF FLAG:10000 >= 20
	SIF (FLAG:8 & 1024) == 0
		FLAG:8 |= 1024
	
	;実績
	LOADGLOBAL
	SIF !(GLOBAL:2300 & 16)
		GLOBAL:2300 |= 16
	SAVEGLOBAL
ENDIF
;過去に1度幻想郷を入手した
SIF FLAG:90 == 5 && (FLAG:8 & 2048) == 0
	FLAG:8 |= 2048
;過去に触手姫エンドを見た
SIF FLAG:10004 > 0 && (FLAG:8 & 4096) == 0
	FLAG:8 |= 4096
;同時に10種以上のハーレムエンドの条件を満たした(bit8192)
;オベロンかティターニアの称号資格を得ている
SIF FLAG:32 >= 1000 && (FLAG:8 & 16384) == 0
	FLAG:8 |= 16384
;[地獄の勲章]を所持
SIF TALENT:MASTER:地獄の勲章 && (FLAG:8 & 32768) == 0
	FLAG:8 |= 32768

;--------------------------------------------------
;助手引き継ぎ処理
;--------------------------------------------------
;キャラが助手可能であり[恋慕]か[服従]か[淫乱]を持っている、かつ好感度か主人調教経験が2000以上を満たしていると
;ニューゲーム以後も助手として引き継がせることが出来るようになる。
;ただし引き継げられる助手の数は[大甲斐性]を持たない場合、最大で9人までとする。
;LOCAL:5～7とTFLAG:0を判定に使用。
;周回数-1だけ引き継ぎ可能人数が増える(最大5人)
LOCAL:6 = LIMIT(FLAG:6 - 1, 0, 5)
;陥落人数の1/10だけ引き継ぎ可能人数が増える(最大4人)
SIF FLAG:30 >= 10
	LOCAL:6 += MIN(FLAG:30 / 10, 4)
;[大甲斐性]を持っていると引き継ぎ可能人数が増加
IF TALENT:MASTER:大甲斐性
	FOR LOCAL, 0, 17
		LOCAL:6 += GETBIT(FLAG:8, LOCAL)
	NEXT
ENDIF
;引き継ぎ人数の限界値設定。なお、デバッグモードのときは20人まで引き継ぎ可能
LOCAL:7 = (TALENT:MASTER:大甲斐性) ? 20 # 9
LOCAL:6 = (TALENT:MASTER:998) ? 20 # LIMIT(LOCAL:6, 0, LOCAL:7)

LOCAL:5 = 0
TFLAG:0 = 0
IF CHARANUM > 1
	FOR LOCAL, 0, CHARANUM
		MARK:LOCAL:98 = 0
		;一応主人自身は排除しておく
		SIF LOCAL == MASTER
			CONTINUE
		;死亡
		SIF TALENT:LOCAL:死亡
			CONTINUE
		;その場にいなければダメ(育児中とか娼館労働中なら問題ない)
		SIF CFLAG:LOCAL:4 == 1 || CFLAG:LOCAL:4 == 4 || CFLAG:LOCAL:4 == 8192
			CONTINUE
		;傀儡、壊造人格、淫魔のいずれかを持ち反発刻印２以下なら引き継げる
		IF (TALENT:LOCAL:傀儡 || TALENT:LOCAL:壊造人格 || TALENT:LOCAL:淫魔) && MARK:LOCAL:反発刻印 < 3
			LOCAL:5 += 1
			CONTINUE
		ENDIF
		;助手可能でなければダメ
		SIF CFLAG:LOCAL:1 < 2
			CONTINUE
		;好感度か主人調教経験のいずれかが2000以上でないとダメ
		SIF CFLAG:LOCAL:好感度 < 2000 && EXP:LOCAL:主人調教経験 < 2000
			CONTINUE
		;恋慕か服従か淫乱がないとダメ
		SIF TALENT:LOCAL:恋慕 || TALENT:LOCAL:服従 || TALENT:LOCAL:淫乱
			LOCAL:5 += 1
	NEXT
	IF LOCAL:5 > 0 && LOCAL:6 > 0
		DRAWLINE
		PRINTFORML 今いるキャラの内誰かを助手として引き継ぐことができます
		PRINTL 助手として引き継ぎますか？
		PRINTLC [0]はい
		PRINTLC [1]いいえ
		PRINTL 
		$INPUT_LOOP_0
		INPUT
		IF RESULT == 0
			DRAWLINE
			TFLAG:0 = 1
		ELSEIF RESULT != 1
			CLEARLINE 1
			REUSELASTLINE
			GOTO INPUT_LOOP_0
		ENDIF
	ENDIF
ENDIF

SIF TFLAG:0 == 1
	CALL TAKEOVER_ASSI, LOCAL:6
SIF TFLAG:0 == 0
	CALL RESET_ASSI

;--------------------------------------------------
;設定引き継ぎ処理
;--------------------------------------------------
;コンフィグのリセット処理
;「TFLAG:1～20にコンフィグの設定をいったん保存」→「アイテム引き継ぎのためにコンフィグの機能を全開放」
;→「アイテムの引き継ぎ処理」→「コンフィグの機能を全部無効」……という流れで行われる
FOR LOCAL, 1, 21
	TFLAG:LOCAL = FLAG:(LOCAL + 9)
NEXT

;複数所持可能アイテムの引き継ぎ(元の所持数の1/5に減少。酒類のみ10本まで引き継ぎ可能)
FOR LOCAL, 0, 610
	IF LOCAL >= 60 && LOCAL < 90
		ITEM:LOCAL = MAX(ITEM:LOCAL / 5, 0)
	ELSEIF LOCAL >= 600 && LOCAL < 610
		ITEM:LOCAL = MIN(ITEM:LOCAL, 10)
	ELSE
		ITEM:LOCAL = 0
	ENDIF
NEXT
;娼館モード
IF FLAG:4 == 4
	ITEM:娼館 = 1
	FLAG:3000 = 0
	FLAG:3100 = 1000
	FLAG:3101 = 1000
ENDIF
;前回の来客数のリセット
FLAG:3007 = 0
FLAG:3008 = 0
;キャラのアイテムカード破棄
FOR LOCAL, 1, 6
	FOR LOCAL:1, 0, 120
		ITEM:(LOCAL * 1000 + LOCAL:1) = 0
	NEXT
NEXT

;助手に誰がいるかを判別
;助手のみキャラカードを取得する処理をここで行う。
FOR LOCAL, 0, CHARANUM
	ITEM:(NO:LOCAL) = (NO:LOCAL < 3000 || NO:LOCAL >= 4000) ? 1 # 0
	CFLAG:LOCAL:401 = 0
	CFLAG:LOCAL:403 = 0
	CFLAG:LOCAL:404 = 0
NEXT
;コンフィグの機能全封印処理
CALL CONFIGURATION_MODE_NONE

;お金の引き継ぎ
LOCAL:1 = MONEY / 20
LOCAL:1 = TABLE_CALC(LOCAL:1, 4000, 2)
LOCAL:1 = TABLE_CALC(LOCAL:1, 10000, 2)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 20000, 75)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 40000, 65)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 70000, 50)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 100000, 25)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 200000, 10)
MONEY = MAX(LOCAL:1, 25000)
;周回を重ねていく毎に引き継いだ分以外にボーナスが入る様に
;ただし引き継ぎボーナスの最高額は10万までとする(そこまで周回を重ねるのも困難な話だが)
LOCAL:1 = MIN(1000 * FLAG:6, 100000)
;娼館モードでは25000がボーナス
SIF FLAG:4 == 4
	LOCAL:1 += 25000
MONEY += LOCAL:1

;蒐集品引き継ぎ処理
;ビット演算でLOCAL:2が1のときはフラグリセット処理において蒐集品フラグが消去されないようになる
LOCAL:2 = 0
IF FLAG:500 > 0
	DRAWLINE
	PRINTL 蒐集品を引き継ぎますか？
	PRINTLC [0]はい
	PRINTLC [1]いいえ
	PRINTL 
	$INPUT_LOOP_1
	INPUT
	IF RESULT == 0
		PRINTW 蒐集品を引き継ぎました
		LOCAL:2 |= 1
	ELSEIF RESULT != 1
		CLEARLINE 1
		REUSELASTLINE
		GOTO INPUT_LOOP_1
	ENDIF
ENDIF
;触手の能力・τの引き継ぎ処理
;予めτ(主人のBASE:触手)を保存し、引き継ぐことを選択しなかった場合は0にする処理
;ビット演算でLOCAL:2が2のときはフラグリセット処理において触手設定のフラグ(触手の能力)が消去されないようになる
IF FLAG:75 > 0
	DRAWLINE
	PRINTL 触手の能力は引き継ぎますか？
	PRINTLC [0]はい
	PRINTLC [1]いいえ
	PRINTL 
	$INPUT_LOOP_2
	INPUT
	IF RESULT == 0
		PRINTW 触手の能力を引き継ぎました
		LOCAL:2 |= 2
	ELSEIF RESULT == 1
		BASE:MASTER:触手 = 0
		DA:1000:15 = 0
	ELSE
		CLEARLINE 1
		REUSELASTLINE
		GOTO INPUT_LOOP_2
	ENDIF
ENDIF
;野菜の引き継ぎ
IF FLAG:574 >= 100
	LOCAL:1 = FLAG:574 / 100
	LOCAL:1 = TABLE_CALC2(LOCAL:1, 1000, 95)
	LOCAL:1 = TABLE_CALC2(LOCAL:1, 2500, 90)
	LOCAL:1 = TABLE_CALC2(LOCAL:1, 5000, 80)
	LOCAL:1 = TABLE_CALC2(LOCAL:1, 10000, 75)
	LOCAL:1 = TABLE_CALC2(LOCAL:1, 25000, 50)
	LOCAL:1 = TABLE_CALC2(LOCAL:1, 50000, 25)
	LOCAL:1 = TABLE_CALC2(LOCAL:1, 100000, 10)
ENDIF
FOR LOCAL, 0, 3000
	IF LOCAL == 3 || LOCAL == 4 || (LOCAL >= 6 && LOCAL <= 11) || (LOCAL >= 18 && LOCAL <= 21) || LOCAL == 29 || LOCAL == 30 || LOCAL == 32 || (LOCAL >= 34 && LOCAL <= 39) || LOCAL == 51 || LOCAL == 70 || LOCAL == 76 || LOCAL == 93 || (LOCAL >= 100 && LOCAL < 420) || LOCAL == 570 || LOCAL == 579 || (LOCAL >= 600 && LOCAL < 603)
		CONTINUE
	ELSEIF (LOCAL == 75 || (LOCAL == 97 && DA:1000:15) || LOCAL == 98 || LOCAL == 99) && LOCAL:2 & 2
		CONTINUE
	;蒐集品フラグリセット(集めた種類)
	ELSEIF LOCAL >= 500 && LOCAL < 520 && LOCAL:2 & 1
		CONTINUE
	;IDの値は引き継いでおく
	ELSEIF LOCAL == 800 || LOCAL == 998 || LOCAL == 999
		CONTINUE
	ELSEIF LOCAL == 2000 || LOCAL == 2001
		CONTINUE
	ELSE
		FLAG:LOCAL = 0
	ENDIF
NEXT
FOR LOCAL, 8000, 9999
	IF LOCAL == 8669 || LOCAL == 9548 || LOCAL == 9549 || LOCAL == 9888
		CONTINUE
	ELSE
		FLAG:LOCAL = 0
	ENDIF
NEXT
;YASAI MODE 判定
SIF TALENT:MASTER:野菜の錬金術師
	FLAG:574 = LOCAL:1
;舊版兼容
FLAG:600 = 20
FLAG:601 = 25

;蒐集品のフラグリセット
IF (LOCAL:2 & 1) == 0
	IF CHARANUM > 1
		;蒐集品採取済みフラグのリセット
		FOR LOCAL, 0, CHARANUM
			CFLAG:LOCAL:10 = 0
			;触手出産回数は触手の能力を引き継ぐ場合のみ保存
			SIF (LOCAL:2 & 2) == 0
				CFLAG:LOCAL:触手出産数 = 0
		NEXT
	ENDIF
	FOR LOCAL:100, 1, 6
		FOR LOCAL, 0, 120
			FOR LOCAL:1, 1, 13
				DA:(1000 * LOCAL:100 + LOCAL):(LOCAL:1) = 0
			NEXT
		NEXT
	NEXT
ENDIF

;周回フラグ
FLAG:6 += 1
;難易度設定
CALL SELECT_NEWTERM_DIFFICULTY

;コンフィグの設定を変更するかどうか決定
PRINTL 
DRAWLINE
PRINTL 追加機能はどうしますか？
PRINTLC [0]今の設定を引き継ぐ
PRINTLC [1]もう一度設定し直す
PRINTLC [2]後で一から設定する
PRINTL 
$INPUT_LOOP_3
INPUT
IF RESULT == 0
	PRINTW 設定を継承します
	FOR LOCAL, 1, 21
		FLAG:(LOCAL + 9) = TFLAG:LOCAL
	NEXT
	CALL CONFIGURATION_8_CHANGE_DEFAULT
ELSEIF RESULT == 1
	FLAG:12 |= 8192
	PRINTW 設定を変更します
	CALL NEWGAME_CONFIGURATION_RESET
ELSEIF RESULT != 2
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP_3
ENDIF



IF FLAG:4 == 1
	;ABNORMALの場合、最初から始めたときに選択した設定を変更するかどうかを訪ねる
	DRAWLINE
	PRINTL 主人の性別・体格等の設定は今のまま引き継ぎますか？
	PRINTLC [0]はい
	PRINTLC [1]いいえ
	PRINTL 
	$INPUT_LOOP_4
	INPUT
	IF RESULT == 0
		PRINTW 主人の性別・体格等の設定を引き継ぎました
	ELSEIF RESULT == 1
		PRINTW これから主人の性別・体格等の設定を行います
		CALL SET_MASTER_SEX
	ELSE
		CLEARLINE 1
		REUSELASTLINE
		GOTO INPUT_LOOP_4
	ENDIF
ENDIF
;PROモードで引き継ぎの際に娼館機能が停止するのを防止するための策
SIF FLAG:4 == 4
	FLAG:17 |= 1
;フラグリセット
VARSET TFLAG, 0
;収容可能人数初期化
FLAG:91 = 1
FLAG:92 = 10

CALL TROPHY_BONUS, 2

PRINTFORMW 強くてニューゲームを始めます　({FLAG:6}周目)
;念のためもう一度リセット
TARGET = -1
ASSI = -1
;引き継ぎ処理実行中の判定ON
TFLAG:994 = 1

;--------------------------------------------------
;難易度設定(周回)
;--------------------------------------------------
@SELECT_NEWTERM_DIFFICULTY
DRAWLINE
PRINTFORML 難易度はどうしますか？\@(TALENT:MASTER:998) ? # (※後からの変更は効きません！)\@
SETCOLOR COLOR("P-GREEN")
PRINTFORML [1]%DISPLAY_DIFFICULTY(1), 9, LEFT%({SET_DEADLINE(1), 3}日期限　目標金額%SHOW_YEN(MONEY:100)%)
SETCOLOR COLOR("aqua")
PRINTFORML [2]%DISPLAY_DIFFICULTY(2), 9, LEFT%({SET_DEADLINE(2), 3}日期限　目標金額%SHOW_YEN(MONEY:100)%)
SETCOLOR COLOR("P-BLUE")
PRINTFORML [3]%DISPLAY_DIFFICULTY(3), 9, LEFT%({SET_DEADLINE(3), 3}日期限　目標金額%SHOW_YEN(MONEY:100)%)
SETCOLOR COLOR("P-RED")
PRINTFORML [4]%DISPLAY_DIFFICULTY(4), 9, LEFT%({SET_DEADLINE(4), 3}日期限　目標金額%SHOW_YEN(MONEY:100)%)
SETCOLOR COLOR("RED")
PRINTFORML [5]%DISPLAY_DIFFICULTY(5), 9, LEFT%({SET_DEADLINE(5), 3}日期限　目標金額%SHOW_YEN(MONEY:100)%)
SIF FLAG:3 >= 4
	PRINTFORML [6]%DISPLAY_DIFFICULTY(6), 9, LEFT%({SET_DEADLINE(6), 3}日期限　目標金額%SHOW_YEN(MONEY:100)%)
SIF FLAG:3 >= 5
	PRINTFORML [7]%DISPLAY_DIFFICULTY(7), 9, LEFT%({SET_DEADLINE(7), 3}日期限　目標金額%SHOW_YEN(MONEY:100)%)
SETCOLOR COLOR("ORANGE")
PRINTL [10]HELP    (各難易度の簡単な説明）
RESETCOLOR
$INPUT_LOOP_2
INPUT
IF RESULT == 10
	DRAWLINE
	SETCOLOR COLOR("P-GREEN")
	PRINTL EASY     ：初心者or練習したい人向け    一部機能制限有り
	SETCOLOR COLOR("aqua")
	PRINTL NORMAL   ：普通にプレイをする人向け    一部機能制限有り
	SETCOLOR COLOR("P-BLUE")
	PRINTL HARD     ：難しめのプレイもOKな人向け
	SETCOLOR COLOR("P-RED")
	PRINTL LUNATIC  ：かなりマゾなプレイヤー向け  特別機能有り
	SETCOLOR COLOR("RED")
	PRINTL PHANTASM ：超弩級なマゾプレイヤー向け  特別機能有り
	SIF FLAG:3 >= 4
		PRINTL ULTRA    ：刻符で悪足掻きたい人向け    特別機能有り
	SIF FLAG:3 >= 5 && FLAG:6 <= 2
		PRINTL OVERDRIVE：理不尽な世界を見たい人向け  特別機能有り
	RESETCOLOR
	DRAWLINE
	WAIT
	DRAWLINE
	DRAWLINE
	DRAWLINE
	RESTART
ELSEIF RESULT == 1
	PRINT 難易度:“
    SETCOLOR COLOR("P-GREEN")
	PRINT EASY
	RESETCOLOR
	PRINTW ”を選択しました
ELSEIF RESULT == 2
	PRINT 難易度:“
    SETCOLOR COLOR("aqua")
	PRINT NORMAL
	RESETCOLOR
	PRINTW ”を選択しました
ELSEIF RESULT == 3
	PRINT 難易度:“
    SETCOLOR COLOR("P-BLUE")
	PRINT HARD
	RESETCOLOR
	PRINTW ”を選択しました
ELSEIF RESULT == 4
	PRINT 難易度:“
    SETCOLOR COLOR("P-RED")
	PRINT LUNATIC
	RESETCOLOR
	PRINTW ”を選択しました
ELSEIF RESULT == 5
	PRINT 難易度:“
    SETCOLOR COLOR("RED")
	PRINT PHANTASM
	RESETCOLOR
	PRINTW ”を選択しました
	;主人の技巧を強制的に1にする
	ABL:MASTER:技巧 = 1
	;禁断の知識没収
	TALENT:MASTER:禁断の知識 = 0
ELSEIF RESULT == 6 && FLAG:3 >= 4
	PRINT 難易度:“
    SETCOLOR COLOR("RED")
	PRINT ULTRA
	RESETCOLOR
	PRINTW ”を選択しました
	;主人の技巧を強制的に1にする
	ABL:MASTER:技巧 = 1
	;禁断の知識没収
	TALENT:MASTER:禁断の知識 = 0
	DRAWLINE
	SETCOLOR COLOR("P-RED")
	ALIGNMENT CENTER
	PRINTL 
	PRINTL “Welcome to Special Round”
	PRINTL 
	RESETCOLOR
	DRAWLINE
	WAIT
	ALIGNMENT LEFT
ELSEIF RESULT == 7 && FLAG:3 >= 5
	PRINT 難易度:“
    SETCOLOR COLOR("RED")
	PRINT OVERDRIVE
	RESETCOLOR
	PRINTW ”を選択しました
	;主人の技巧を強制的に1にする
	ABL:MASTER:技巧 = 1
	;禁断の知識没収
	TALENT:MASTER:禁断の知識 = 0
	DRAWLINE
	SETCOLOR COLOR("RED")
	ALIGNMENT CENTER
	PRINTL 
	PRINTL “地獄へようこそ”
	PRINTL 
	RESETCOLOR
	DRAWLINE
	WAIT
	ALIGNMENT LEFT
ELSE
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP_2
ENDIF
FLAG:3 = RESULT
;期限設定
FLAG:999 = SET_DEADLINE(FLAG:3)

;=============================================================================
;周回における主人の素質取得・変更処理
;=============================================================================
;--------------------------------------------------
;主人の素質追加処理
;--------------------------------------------------
;いいえを選択しても壊造人格を『覚えさせたことがある』と判定される
@NEWTERM_TALENT_MASTER, ARG
DRAWLINE
PRINTFORMW [%TALENTNAME:ARG%]を習得することができます
PRINTL 習得しますか？
PRINTLC [0]はい
PRINTLC [1]いいえ
PRINTL 
$INPUT_LOOP
INPUT
IF RESULT == 0
	PRINTFORMW %CALLNAME:MASTER%は[%TALENTNAME:ARG%]を身につけた
	TALENT:MASTER:ARG = 1
ELSEIF RESULT != 1
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP
ENDIF

;--------------------------------------------------
;主人の種族変更処理
;--------------------------------------------------
@CHANGE_RACE_MASTER
#LOCALSIZE 2
PRINTL 主人の種族を変更することができます
PRINTL 変更しますか？
PRINTLC [0]はい
PRINTLC [1]いいえ
PRINTL 
$INPUT_LOOP
INPUT
IF RESULT == 0
	PRINTW 主人の種族を変更します
	CALL RACE_MASTER_LIST
ELSEIF RESULT == 1
	PRINTW 主人の種族を変更しません
	RETURN 0
ELSE
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP
ENDIF

;………………………………………………
;主人の種族選択
;………………………………………………
@RACE_MASTER_LIST
PRINTL 主人の種族を選択してください
DRAWLINE
IF FLAG:400 > 99 && CFLAG:MASTER:5
	PRINTLC [00] 元に戻る
ELSE
	PRINTL ※最悪元に戻れなくなる可能性があります
ENDIF
SIF FLAG:70 & 1
	PRINTLC [01] 妖精
SIF FLAG:70 & 2
	PRINTLC [02] 吸血鬼
SIF FLAG:70 & 4
	PRINTLC [03] 機械
SIF FLAG:70 & 8
	PRINTLC [04] 亡霊
SIF FLAG:70 & 16
	PRINTLC [05] 人形
SIF FLAG:70 & 32
	PRINTLC [06] 悪魔
SIF FLAG:70 & 64
	PRINTLC [07] 河童
SIF FLAG:70 & 128
	PRINTLC [08] 妖狐
SIF FLAG:70 & 256
	PRINTLC [09] ワーハクタク
SIF FLAG:70 & 512
	PRINTLC [10] ゾンビフェアリー
SIF FLAG:70 & 1024
	PRINTLC [11] キョンシー
PRINTLC [100]やめる
PRINTL 
DRAWLINE
$INPUT_LOOP
INPUT
IF RESULT == 100
	PRINTW 主人の種族を変更しません
	RETURN 0
ELSEIF RESULT == 0 && FLAG:400 > 99 && CFLAG:MASTER:5
	PRINTW 主人の種族を元に戻しました
	LOCAL = 0
ELSEIF RESULT == 1 && FLAG:70 & 1
	LOCAL = 200
	LOCAL:1 = 3203
ELSEIF RESULT == 2 && FLAG:70 & 2
	LOCAL = 205
	LOCAL:1 = 3206
ELSEIF RESULT == 3 && FLAG:70 & 4
	LOCAL = 206
	LOCAL:1 = 3246
ELSEIF RESULT == 4 && FLAG:70 & 8
	LOCAL = 207
	LOCAL:1 = 3211
ELSEIF RESULT == 5 && FLAG:70 & 16
	LOCAL = 208
	LOCAL:1 = 3221
ELSEIF RESULT == 6 && FLAG:70 & 32
	LOCAL = 209
	LOCAL:1 = 3204
ELSEIF RESULT == 7 && FLAG:70 & 64
	LOCAL = 210
	LOCAL:1 = 3227
ELSEIF RESULT == 8 && FLAG:70 & 128
	LOCAL = 211
	LOCAL:1 = 3212
ELSEIF RESULT == 9 && FLAG:70 & 256
	;ワーハクタクは専用処理するのでLOCAL:1は設定しない
	LOCAL = 212
ELSEIF RESULT == 10 && FLAG:70 & 512
	LOCAL = 48
	LOCAL:1 = 3245
ELSEIF RESULT == 11 && FLAG:70 & 1024
	LOCAL = 226
	LOCAL:1 = 3261
ELSE
	RESTART
ENDIF

;種族変更フラグセット
CFLAG:MASTER:5 = RESULT

;種族遺伝子を一旦全て初期化
VARSET CFLAG:MASTER:0, 0, 3200, 3400

;[呪精]リセット
TALENT:MASTER:呪精 = 0

;種族素質全てリセット
VARSET TALENT:MASTER:処女, 0, 200, 226

;人間(？)に戻る
IF LOCAL == 0
	CFLAG:MASTER:3200 = 100
	RETURN 0
;種族遺伝子セット。ワーハクタクだけ専用処理
ELSEIF LOCAL == 212
	CFLAG:MASTER:3201 = 50
	CFLAG:MASTER:3253 = 50
ELSE
	CFLAG:MASTER:(LOCAL:1) = 100
ENDIF

IF CFLAG:MASTER:3200 != 100
	;実績
	LOADGLOBAL
	SIF !(GLOBAL:2300 & 4096)
		GLOBAL:2300 |= 4096
	SAVEGLOBAL
ENDIF

;種族別素質セット
TALENT:MASTER:LOCAL = 1
PRINTFORMW %CALLNAME:MASTER%の種族を[%TALENTNAME:LOCAL%]に変更しました

;--------------------------------------------------
;主人の素質(蓬莱人)引き継ぎ処理
;--------------------------------------------------
@TAKEOVER_TALENT_HOURAI, ARG
DRAWLINE
PRINTFORMW 素質[%TALENTNAME:ARG%]を引き継ぐことができます
PRINTFORML [%TALENTNAME:ARG%]になりますか？
PRINTLC [0]はい
PRINTLC [1]いいえ
PRINTL 
$INPUT_LOOP
INPUT
IF RESULT == 0
	PRINTFORMW %CALLNAME:MASTER%は永遠の命を得た
	TALENT:MASTER:ARG = 1
ELSEIF RESULT == 1
	PRINTFORMW %CALLNAME:MASTER%は限りある生を楽しむことにした
	TALENT:MASTER:ARG = 0
ELSE
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP
ENDIF

;--------------------------------------------------
;開発した風呂と触手の能力の総数
;--------------------------------------------------
@CHECK_BATH_AND_TENTACLE
#LOCALSIZE 1
;触手の能力の数
LOCAL = SPECIALTENTACLE_CHECK()
;キャラ触手の数
LOCAL += CHARATENTACLE_CHECK()
;特殊風呂の数
LOCAL += SPECIALBATH_CHECK()
;合計45以上なら付与
SIF LOCAL >= 45
	FLAG:8 |= 256

;=============================================================================
;助手を引き継がないときの処理
;=============================================================================
@RESET_ASSI
PICKUPCHARA MASTER

;念のためにもう一度チェック
SIF CHARANUM > 1
	RESTART

;=============================================================================
;助手引き継ぎ処理
;=============================================================================
;MARK:98が3のキャラは引き継ぎ対象としてマーク
;引き継ぎ可能にするには助手可能で好感度が2000以上、[恋慕]か[服従]か[淫乱]を持っている必要がある
;また引き継ぎできる人数は通常で最大9人である(ただし特定条件を満たすと20人まで引き継ぎ可能……にする予定)
;LOCALは引き継ぐことができる助手数、LOCAL:1は選択している助手数を示す
@TAKEOVER_ASSI, ARG
VARSET LOCAL, 0
$TAKEOVER_ASSI_START
;表示させるキャラを抽出（LOCAL:2に人数）
CALLF CLEAR_LIST
LOCAL:2 = 0
LOCAL:3 = 0
REPEAT CHARANUM
	;選択した助手はそのままの判定
	SIF MARK:COUNT:98 != 3
		MARK:COUNT:98 = 0
	;主人、あるいはその場にいない助手はここで弾く
	SIF COUNT == MASTER || CFLAG:COUNT:4 == 1 || CFLAG:COUNT:4 == 4 || CFLAG:COUNT:4 == 8192
		CONTINUE
	;[死亡]キャラもここで弾く
	SIF TALENT:COUNT:死亡
		CONTINUE
	;[Debug]付加キャラもここで弾く
	SIF TALENT:COUNT:998
		CONTINUE
	;[傀儡]か[壊造人格]か[淫魔]持ちの場合は反発刻印3以下ならOK
	IF TALENT:COUNT:傀儡 == 0 && TALENT:COUNT:壊造人格 == 0 && TALENT:COUNT:淫魔 == 0 && MARK:COUNT:反発刻印 < 3
		;助手可能である
		SIF CFLAG:COUNT:1 < 2
			CONTINUE
		;好感度が2000以上か主人調教経験2000以上必要
		SIF CFLAG:COUNT:好感度 < 2000 && EXP:COUNT:主人調教経験 < 2000
			CONTINUE
		;[恋慕]か[服従]か[淫乱]を持っている
		SIF TALENT:COUNT:恋慕 == 0 && TALENT:COUNT:服従 == 0 && TALENT:COUNT:淫乱 == 0
			CONTINUE
	ENDIF

	CALLF SET_LIST, LOCAL:2, COUNT
	LOCAL:2 += 1
	SIF MARK:COUNT:98 != 3
		MARK:COUNT:98 = 1
REND
LOCAL:3 = (LOCAL:2 - 1) / 20
$TAKEOVER_ASSI_PRINT
DRAWLINE
PRINTFORML 後{ARG-LOCAL:1}人引き継ぐことができます ＜page.{LOCAL+1}＞
DRAWLINE
CALL TAKEOVER_ASSI_LIST, LOCAL
DRAWLINE
PRINTFORMLC \@(LOCAL <= 0) ? %" " * 16% # [1001]前のページ\@
PRINTLC [1000]おわり
PRINTFORMLC \@(LOCAL >= LOCAL:3) ? %" " * 16% # [1009]次のページ\@
PRINTL 
$INPUT_LOOP_0
INPUT
IF RESULT == 1000
	LOCAL = 0
ELSEIF RESULT == 1001 && LOCAL > 0
	LOCAL -= 1
	GOTO TAKEOVER_ASSI_PRINT
ELSEIF RESULT == 1009 && LOCAL < LOCAL:3
	LOCAL += 1
	GOTO TAKEOVER_ASSI_PRINT
ELSEIF RESULT < 1 || RESULT >= CHARANUM || MARK:RESULT:98 == 0
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP_0
ELSE
	IF MARK:RESULT:98 == 3
		PRINTFORMW %CALLNAME:RESULT%を引き継ぎの対象から外しました
		LOCAL:1 -= 1
		MARK:RESULT:98 = 0
	ELSEIF ARG == LOCAL:1
		PRINTW これ以上引き継ぎ対象を選択することができません
	ELSE
		PRINTFORMW %CALLNAME:RESULT%を引き継ぎの対象に選択しました
		LOCAL:1 += 1
		MARK:RESULT:98 = 3
	ENDIF
	GOTO TAKEOVER_ASSI_START
ENDIF

IF LOCAL:1 == 0
	PRINTL 誰も引き継ぎ対象に選択していません
	PRINTL よろしいですか？
ELSE
	REPEAT CHARANUM
		SIF MARK:COUNT:98 == 3
			PRINTFORM [%CALLNAME:COUNT%] 
	REND
	PRINTL 
	PRINTL 以上を助手として引き継ぎますか？
ENDIF
PRINTLC [0]はい
PRINTLC [1]いいえ
PRINTL 
$INPUT_LOOP_1
INPUT
IF RESULT == 1
	GOTO TAKEOVER_ASSI_START
ELSEIF RESULT != 0
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP_1
ENDIF

;--------------------------------------------------
;奴隷消去処理
;--------------------------------------------------
IF LOCAL:1 == 0
	TFLAG:0 = 0
	RETURN 0
ENDIF
LOCAL:3 = 0
$DEL_SLAVE
REPEAT CHARANUM
	;主人自身は消去しちゃらめぇえええ！
	SIF COUNT == MASTER
		CONTINUE
	IF MARK:COUNT:98 == 3
		MARK:COUNT:98 = 2
		LOCAL:3 += 1
		BREAK
	ELSEIF MARK:COUNT:98 < 2
		DELCHARA COUNT
		BREAK
	ENDIF
REND
;処理が引き継ぐ予定の人数に達していないまたは処理が完了していない場合はもう一度処理を繰り返す
SIF LOCAL:3 < LOCAL:1 || CHARANUM > LOCAL:1 + 1
	GOTO DEL_SLAVE

;10人以上引き継いだ場合は購入できる奴隷数追加のため、ここで土地を追加させる
SIF LOCAL:3 > 9
	FLAG:93 = LOCAL:3 - 9

;--------------------------------------------------
;助手の能力引き継ぎ用リセット処理
;--------------------------------------------------
FOR ASSI, 0, CHARANUM
	;主人は飛ばす
	SIF ASSI == MASTER
		CONTINUE
	LOCAL:4 = 0
	;引き継ぎ操作の前に予め桃の飴玉を飲んでいるとＥＸ化の解除選択できるようになる
	SIF EQUIP:ASSI:桃の飴玉 == 1
		CALL RESET_EX_ASSI
	;助手が即落ちを持っていない＋(過去の周回プレイを含め)今までに淫乱、淫核／淫茎、淫壷、淫尻、淫乳、尿道狂、キス魔、ドＳ、ドＭを全て([オトコ]の場合、[淫壷]は不要)取得、
	;かつ今回の周回プレイで難易度HARD以上をクリアした場合、または一度でも即落ちを覚えさせた場合最初から即落ちが付加されるようになる。
	IF ((TALENT:ASSI:オトコ && (CFLAG:ASSI:16 & 507) == 507) || (CFLAG:ASSI:16 & 511) == 511) || (CFLAG:ASSI:16 & 512); && TALENT:ASSI:即落ち == 0)
		CALL FALLEN_ASSI, LOCAL:4
		LOCAL:4 = RESULT
	ENDIF
	;助手が壊造人格を持っていない＋(過去の周回プレイを含め)今までに幼児退行、狂気、精神崩壊、傀儡を全て取得し、記憶消去薬を1回以上飲ませ、
	;かつ今回の周回プレイで難易度HARD以上をクリアした場合、または一度でも壊造人格を覚えさせた場合最初から壊造人格が付加されるようになる。
	IF ((CFLAG:ASSI:17 == 31) || CFLAG:ASSI:17 & 32); && TALENT:ASSI:壊造人格 == 0
		CALL CONVERTED_ASSI, LOCAL:4
		LOCAL:4 = RESULT
	ENDIF
	;助手がオトコでも生娘ではなく、好感度が1万以上、現在[相愛]を持っている＋(過去の周回プレイを含め)今までに恋慕、妄信、親愛、相愛を全て取得し、
	;かつ今回の周回プレイで難易度HARD以上をクリアした場合、または一度でも生娘化を実行した場合、生娘化にするか選択できるようになる。
	SIF (((CFLAG:ASSI:14 & 15) > 0) || CFLAG:ASSI:14 & 16) && CFLAG:ASSI:好感度 > 9999 && TALENT:ASSI:処女 != 1 && TALENT:ASSI:オトコ == 0 && TALENT:ASSI:相愛 && FLAG:3 >= 3
		CALL VIRGIN_ASSI

	;専用口上を使用と設定している、かつその口上での特殊引き継ぎ設定処理があった場合はそちらの処理を優先。それ以外は通常の処理
	IF CFLAG:ASSI:9 & 8
		TRYCCALLFORM KOJO_{NO:ASSI}_EVENT_ASSI_TAKEOVER, LOCAL:4
		CATCH
			CALL ASSI_STATE_TAKEOVER, LOCAL:4
		ENDCATCH
	ELSE
		CALL ASSI_STATE_TAKEOVER, LOCAL:4
	ENDIF
NEXT

;削除処理防止用
TFLAG:0 = 1

;--------------------------------------------------
;助手引き継ぎ一覧表示
;--------------------------------------------------
@TAKEOVER_ASSI_LIST, ARG
REPEAT 20
	LOCAL = GET_LIST(COUNT + ARG * 20)
	SIF LOCAL == 0
		CONTINUE
	;主人公と助手対象と調教中キャラは排除
	SIF LOCAL == 0
		CONTINUE
	SIF CFLAG:LOCAL:4 == 1 || CFLAG:LOCAL:4 == 8192
		CONTINUE
	IF MARK:LOCAL:98 == 3
		PRINT ★
	ELSE
		PRINT 　
	ENDIF
	CALL ARRANGE_CHARALIST, LOCAL
	CALL ARRANGE_CHARALIFE, LOCAL
	PRINTL 
REND
PRINTL 　　　★：助手として引き継ぐ予定のキャラを表しています

;=============================================================================
;助手引き継ぎ用処理
;=============================================================================
;--------------------------------------------------
;助手の能力引き継ぎ用リセット処理
;--------------------------------------------------
;そのままでスタートするにはゲームに不都合な部分をここで処理
@ASSI_STATE_TAKEOVER, ARG
;助手を経験したことを示す
ISASSI:ASSI = 1
;引き継いだキャラであることを判別するためのフラグ
CFLAG:ASSI:18 = 1
;称号リセット
NICKNAME:ASSI = %CSVNICKNAME (NO:ASSI , 0)%
;館にいた時の状況
CSTR:ASSI:3 = 過去において%CALLNAME:MASTER%の調教を受けた

;妊娠・抱卵中・乳母でなく育児中の場合は減っている体力を戻す
IF TALENT:ASSI:妊娠 || TALENT:ASSI:抱卵中 || (TALENT:ASSI:育児中 && CFLAG:ASSI:79 == 0)
	MAXBASE:ASSI:0 += 500
	CALL RESET_ASSI_PREGNANT, ASSI
;懐卵時の体力の最大値低下を戻す処理
ELSEIF TALENT:ASSI:懐卵
	MAXBASE:ASSI:0 += 500
ENDIF
;理性ゲージ設定
IF CSVBASE(NO:ASSI, 9, 0) > 0
	MAXBASE:ASSI:9 = CSVBASE(NO:ASSI, 9, 0)
ELSE
	MAXBASE:ASSI:9 = 9500 + 25 * RAND:101
ENDIF
BASE:ASSI:理性 = MAXBASE:ASSI:9
TALENT:ASSI:理性 = 1
;調教度ゲージ設定
CALL CHECK_ACCEPTANCE,ASSI
;初期月経周期を設定
CALL EVENT_ADD_CHARA_MENSTRUATION, ASSI

;特定のゲージをリセット
REPEAT 4
	MAXBASE:ASSI:(COUNT+4) = 0
	BASE:ASSI:(COUNT+4) = 0
REND
;居場所のフラグリセット
CFLAG:ASSI:4 = 0
;恐怖・反発刻印取得済み判定リセット
MARK:ASSI:96 = 0
MARK:ASSI:97 = 0
;薬物刻印1Lv分減少
MARK:ASSI:薬物刻印 = MAX(MARK:ASSI:薬物刻印 - 1, 0)

;特定の素質をリセット
FOR LOCAL, 140, 180
	;壊造人格、即落ちは引き継ぎで取得させていない場合はリセット
	IF (LOCAL == 169 && (ARG & 2) == 0) || (LOCAL == 179 && (ARG & 1) == 0)
		TALENT:ASSI:LOCAL = 0
	;妊娠、育児中、母性、抱卵中、寄生、懐卵はリセット(バグ防ぐため)
	ELSEIF (LOCAL >= 140 && LOCAL <= 143) || LOCAL == 145 || LOCAL == 146
		TALENT:ASSI:LOCAL = 0
	;恋慕系、服従系、淫乱系もリセット(特に恋慕、淫乱、服従は堕とした人数のカウントに影響されるため)
	ELSEIF (LOCAL >= 150 && LOCAL < 157) || (LOCAL >= 159 && LOCAL < 169) || (LOCAL >= 170 && LOCAL < 179)
		TALENT:ASSI:LOCAL = 0
	ENDIF
NEXT
TALENT:ASSI:パイパン = 0
TALENT:ASSI:娼婦 = 0
TALENT:ASSI:求愛依存 = 0
TALENT:ASSI:売春中毒 = 0
TALENT:ASSI:看板娘 = 0
TALENT:ASSI:常連客 = 0
TALENT:ASSI:常時発情 = 0
TALENT:ASSI:998 = 0

;CFLAGをリセット
FOR LOCAL, 0, 100
	;売却不可フラグ、ＶＡ異常経験変動フラグリセット
	IF LOCAL == 3 || LOCAL == 11
		CFLAG:ASSI:LOCAL = 0
	;嫉妬、放尿レベル、媚薬中毒、鈴蘭の毒、陰毛、触手インプラント、烙印用装備フラグはリセット
	ELSEIF (LOCAL >= 30 && LOCAL <= 33) || LOCAL == 36 || LOCAL == 41 || LOCAL == 42
		CFLAG:ASSI:LOCAL = 0
	;汎用口上、逆口上の人格再設定用
	ELSEIF LOCAL == 43 || LOCAL == 44
		CFLAG:ASSI:LOCAL = 0
	;最後の調教を受けた日と最後に助手を外された日をリセット（しないと表示が不自然になる）
	ELSEIF LOCAL == 46 || LOCAL == 47
		CFLAG:ASSI:LOCAL = 0
	;部位開発度、強制精飲絶頂フラグはリセット
	ELSEIF (LOCAL >= 60 && LOCAL <= 64)
		CFLAG:ASSI:LOCAL = 0
	;娼婦人気もリセット
	ELSEIF LOCAL == 66
		CFLAG:ASSI:LOCAL = 0
	;妊娠出産系フラグも全部リセット
	ELSEIF LOCAL >= 70 && LOCAL <= 79
		CFLAG:ASSI:LOCAL = 0
	ENDIF
NEXT
FOR LOCAL, 1000, 15100
	;娘に関するフラグを保存
	SIF LOCAL >= 3100 && LOCAL < 3110
		CONTINUE
	;種族遺伝子に関するフラグを保存
	SIF LOCAL >= 3200 && LOCAL < 3400
		CONTINUE
	;性別反転した時のもとの素質に関するフラグを保存
	SIF LOCAL >= 4995 && LOCAL < 5000
		CONTINUE
	CFLAG:ASSI:LOCAL = 0
NEXT

;EQUIPをリセットする
VARSET EQUIP:ASSI:桃の飴玉, 0, 0, 15

;好感度は減少させた状態で引き継ぎ
;最低は50(引き継ぐ時点で1000以上はあると見込んでいるため)
;素質が変動する700、1000、2000、5000を境界に引き継ぎ率が激減するように
LOCAL:1 = CFLAG:ASSI:好感度 / 20
LOCAL:1 = TABLE_CALC2(LOCAL:1, 100, 90)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 250, 80)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 500, 75)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 700, 66)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 1000, 50)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 2000, 33)
LOCAL:1 = TABLE_CALC2(LOCAL:1, 5000, 25)
CFLAG:ASSI:好感度 = LOCAL:1

;--------------------------------------------------
;助手の胸を元に戻す処理
;--------------------------------------------------
;[妊娠]等による胸の大きさの変動をここで修正
@RESET_ASSI_PREGNANT, ARG
;胸の素質が変化していないなら帰る
SIF !CFLAG:ARG:3006
	RETURN 0

;一旦胸の素質をリセット
TALENT:ARG:絶壁 = 0
TALENT:ARG:貧乳 = 0
TALENT:ARG:巨乳 = 0
TALENT:ARG:爆乳 = 0

;拡大前の状態に戻す
;絶壁だった
IF CFLAG:ARG:3006 == 1
	TALENT:ARG:絶壁 = 1
;貧乳だった
ELSEIF CFLAG:ARG:3006 == 2
	TALENT:ARG:貧乳 = 1
;巨乳だった
ELSEIF CFLAG:ARG:3006 == 4
	TALENT:ARG:巨乳 = 1
;爆乳だった
ELSEIF CFLAG:ARG:3006 == 5
	TALENT:ARG:爆乳 = 1
ENDIF

;--------------------------------------------------
;助手のＥＸ化解除選択処理
;--------------------------------------------------
@RESET_EX_ASSI
PRINTFORMW ＜桃の飴玉の効果で%CALLNAME:ASSI%のＥＸ化状態を解除することができます＞
PRINTFORML ＜%CALLNAME:ASSI%のＥＸ化状態を解除しますか？＞
PRINTLC [0]はい
PRINTLC [1]いいえ
PRINTL 
$INPUT_LOOP_RESET_EX_ASSI
INPUT
IF RESULT == 0
	CALL RESET_EX_COMMON, ASSI
	PRINTFORMW %CALLNAME:ASSI%のＥＸ化状態を解除しました
ELSEIF RESULT == 1
	PRINTFORMW %CALLNAME:ASSI%のＥＸ化状態を解除することを見送りました
ELSE
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP_RESET_EX_ASSI
ENDIF
;桃の飴玉の効果が切れる
EQUIP:ASSI:桃の飴玉 = 0

;--------------------------------------------------
;助手に[即落ち]を追加させる処理
;--------------------------------------------------
;いいえを選択しても即落ちを『覚えさせたことがある』と判定される
;(つまりこの選択肢を出した時点で淫乱系素質全取得の条件が1つ満たされることに)
@FALLEN_ASSI, ARG
PRINTFORMW %CALLNAME:ASSI%のこちらを見る目がおかしい…
PRINTFORMW 幾度となく調教を施された%CALLNAME:ASSI%の身体は、完全に%CALLNAME:MASTER%の手に堕ちてしまったようだ……
PRINTFORMW ＜%CALLNAME:MASTER%の手によってより感じやすい体に変えることができます＞
PRINTFORML ＜%CALLNAME:ASSI%に[即落ち]を覚えさせますか？＞
SIF (CFLAG:ASSI:16 & 512) == 0
	PRINTL (※いいえを選んでも即落ちを『覚えさせたことがある』と判定されます)
PRINTLC [0]はい
PRINTLC [1]いいえ
PRINTL 
$INPUT_LOOP_FALLEN_ASSI
INPUT
IF RESULT == 0
	PRINTFORMW %CALLNAME:ASSI%は[即落ち]を得た
	TALENT:ASSI:即落ち = 1
	ARG |= 1
ELSEIF RESULT == 1
	PRINTFORMW %CALLNAME:ASSI%に[即落ち]を覚えさせることを見送りました
ELSE
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP_FALLEN_ASSI
ENDIF
CFLAG:ASSI:16 |= 512

RETURN ARG

;--------------------------------------------------
;助手に[壊造人格]を追加させる処理
;--------------------------------------------------
;いいえを選択しても壊造人格を『覚えさせたことがある』と判定される
@CONVERTED_ASSI, ARG
PRINTFORMW %CALLNAME:ASSI%は濁ったガラス玉のような瞳でこちらを見つめている…
PRINTFORMW 幾度となく過酷な調教を施された%CALLNAME:ASSI%の人格は、跡形もなく破壊されてしまったようだ……
PRINTFORMW ＜%CALLNAME:MASTER%の手によって新しい人格を植え付けることができます＞
PRINTFORML ＜%CALLNAME:ASSI%に[壊造人格]を覚えさせますか？＞
SIF (CFLAG:ASSI:17 & 32) == 0
	PRINTL (※いいえを選んでも壊造人格を『覚えさせたことがある』と判定されます)
PRINTLC [0]はい
PRINTLC [1]いいえ
PRINTL 
$INPUT_LOOP_CONVERTED_ASSI
INPUT
IF RESULT == 0
	PRINTFORMW %CALLNAME:ASSI%は%CALLNAME:MASTER%によって新しい人格を上書きされました
	PRINTFORMW %CALLNAME:ASSI%は[壊造人格]を得た
	TALENT:ASSI:壊造人格 = 1
	NICKNAME:MASTER = 『鬼畜』
	CFLAG:MASTER:157 |= 8
	;主人との相性が500％に(解釈は各自ご自由に)
	IF RELATION:ASSI:(NO:MASTER) < 500
		RELATION:ASSI:(NO:MASTER) = 500
		PRINTFORMW %CALLNAME:ASSI%の魂は永遠に%CALLNAME:MASTER%の手から逃れることができなくなりました
	ENDIF
	ARG |= 2
	;実績
	SIF !(GLOBAL:2300 & 16384)
		GLOBAL:2300 |= 16384
	SAVEGLOBAL
ELSEIF RESULT == 1
	PRINTFORMW %CALLNAME:ASSI%に[壊造人格]を覚えさせることを見送りました
ELSE
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP_CONVERTED_ASSI
ENDIF
SIF (CFLAG:ASSI:17 & 32) == 0
	FLAG:37 += 1
CFLAG:ASSI:17 |= 32

RETURN ARG

;--------------------------------------------------
;助手が生娘化する処理
;--------------------------------------------------
@VIRGIN_ASSI
PRINTFORMW %CALLNAME:ASSI%の手にはいつの間にか美しくも儚げに輝く宝珠が握られていた…
PRINTFORMW これは%CALLNAME:ASSI%へ深い愛情を注いだことによる奇跡なのだろうなのか？
PRINTW それとも――
PRINTW ……
PRINTW …………
PRINTW ………………
PRINTFORMW ＜宝珠の力を解放することで%CALLNAME:ASSI%を[生娘]に戻すことができます＞
PRINTFORML ＜%CALLNAME:ASSI%を[生娘]にしますか？＞
SIF EXP:ASSI:Ｖ経験 || EXP:ASSI:Ｖ拡張経験 || EXP:ASSI:Ｖ調律経験 || EXP:ASSI:産卵経験 || EXP:ASSI:出産経験 || CFLAG:ASSI:触手出産数
	PRINTL (※はいを選ぶと[処女]を得る代わりに関連するＶ経験は全てリセットされます)
PRINTLC [0]はい
PRINTLC [1]いいえ
PRINTL 
$INPUT_LOOP_VIRGIN_ASSI
INPUT
IF RESULT == 0
	PRINTFORMW %CALLNAME:ASSI%の身体は宝珠の力によって生娘に戻りました
	PRINTFORMW %CALLNAME:ASSI%は[処女]を得た
	TALENT:ASSI:処女 = 1
	CFLAG:ASSI:14 |= 16
	IF ABL:ASSI:Ｖ感覚 || EXP:ASSI:Ｖ経験 || EXP:ASSI:Ｖ拡張経験 || EXP:ASSI:Ｖ調律経験 || EXP:ASSI:産卵経験 || EXP:ASSI:出産経験 || EXP:ASSI:被強姦経験 || EXP:ASSI:被輪姦経験 || EXP:ASSI:被姦快楽経験 || EXP:ASSI:別人調教経験 || CFLAG:ASSI:触手出産数
		PRINTFORMW 宝珠の力で%CALLNAME:ASSI%の膣は[生娘]のそれに変化した
		ABL:ASSI:Ｖ感覚 = 0
		EXP:ASSI:Ｖ経験 = 0
		EXP:ASSI:Ｖ拡張経験 = 0
		EXP:ASSI:Ｖ調律経験 = 0
		EXP:ASSI:産卵経験 = 0
		EXP:ASSI:出産経験 = 0
		EXP:ASSI:被強姦経験 = 0
		EXP:ASSI:被輪姦経験 = 0
		EXP:ASSI:被姦快楽経験 = 0
		EXP:ASSI:別人調教経験 = 0
		CFLAG:ASSI:触手出産数 = 0
	ENDIF
ELSEIF RESULT == 1
	PRINTFORMW %CALLNAME:ASSI%を[生娘]に戻すことを見送りました
ELSE
	CLEARLINE 1
	REUSELASTLINE
	GOTO INPUT_LOOP_VIRGIN_ASSI
ENDIF
