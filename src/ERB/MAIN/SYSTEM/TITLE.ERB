@SYSTEM_TITLE
[IF_DEBUG]
SIF LINECOUNT > 1
	WAIT
[ENDIF]
$PRINT_TITLE
RESETCOLOR
CLEARLINE LINECOUNT
DRAWLINE
ALIGNMENT CENTER
PRINTFORM %GAMEBASE_TITLE%
PRINTL 
LOCALS = {GAMEBASE_VERSION}
SUBSTRING LOCALS, 1, 3
SETCOLOR 0x666666
PRINT Ver 
PRINTFORML {GAMEBASE_VERSION / 1000}.%RESULTS%
RESETCOLOR
PRINTFORML %GAMEBASE_AUTHOR%
SIF STRLENS(GAMEBASE_YEAR) > 0
	PRINTFORML (%GAMEBASE_YEAR%)
PRINTFORML
PRINTL ※当バリアントは調教SLG作成ツールerakanonを改変・再配布したものです。
PRINTL ※18才未満または社会的常識に基づいた行動がとれない方はプレイしないで下さい。
;PRINTFORML %GAMEBASE_INFO%
DRAWLINE
PRINTL

CALL TITLE_ANIMATION
SIF RESULT != 0
	GOTO PRINT_TITLE

;--------------------------------------------------
;any key answer (Press any key)
;反転周期35
;ARG:1 (1=アニメーションオフ時は非表示)
;ARG:2 (1:bluelish + 点滅)
;--------------------------------------------------
@TITLE_ANIMATION
#DIM select
LOCAL = 0
LOCAL:1 = 0
LOCAL:2 = 0

PRINTL 
PRINTL 
;アニメーション
$REPEAT_LOOP
REDRAW 0
	CLEARLINE 2
	ALIGNMENT CENTER
	PRINTL [0] 続きから
	PRINTL
	PRINTL [1] 最初から
	PRINTL
	PRINTL [2] 実績確認
	;PRINTL [2] ゲームを終了する  
	PRINTL
	
	SETCOLOR 0x888888
	ALIGNMENT RIGHT
	PRINTFORM  %" "*10% [9] 
	PRINTFORML 更新履歴 　　
	RESETCOLOR
	ALIGNMENT LEFT

	$INPUT_LOOP
	ALIGNMENT CENTER

	LOCAL:2 = 0x005599 + 65536*3*LOCAL + 256*3*LOCAL - 3*LOCAL
	;輝度補正
	LOCAL:2 += 65536*2*LOCAL + 256*2*LOCAL + 2*LOCAL
	
;	SIF (LOCAL:1) == 0 && (LOCAL == 25 || LOCAL == 30)
;		LOCAL:2 -= 256*10 + 80 - 65536*70
;	SIF (LOCAL:1) == 0 && ((LOCAL > 25 && LOCAL < 28) || (LOCAL > 30 && LOCAL < 35))
;		LOCAL:2 -= 256*20 + 60 - 65536*60
	SETCOLOR LOCAL:2
	GETCOLOR
	IF (LOCAL:1) == 0
		LOCAL += 1
		SIF LOCAL == 34
			LOCAL:1 = 1
	ELSEIF (LOCAL:1) == 1
		LOCAL -= 1
		SIF LOCAL == 0
			LOCAL:1 = 0
	ENDIF

	PRINT Press to Select the mode
	PRINTL
	TINPUT 50, -999, 0
	CLEARLINE 8
	RESETCOLOR
	SIF RESULT == -999
		GOTO REPEAT_LOOP
ALIGNMENT LEFT
REDRAW 0
LOCAL = 0
IF RESULT == 1
	CLEARLINE 2
	PRINTL 
	ALIGNMENT CENTER
	LOADGLOBAL
	PRINTBUTTON "[1] 最初から",""
	PRINTL 
	ALIGNMENT LEFT
	PRINTL 
	LOCAL = 1
	RESETDATA
	ADDCHARA 1000
	BEGIN FIRST
ELSEIF RESULT == 0
	PRINTL 
	DRAWLINE
	LOADGLOBAL
	LOADGAME
	GOTO SKIP_RESTART
ELSEIF RESULT == 2
    CLEARLINE 2
	PRINTL 
	ALIGNMENT CENTER
	PRINTBUTTON "[2] 実績確認",""
	PRINTL 
	PRINTL 
	ALIGNMENT LEFT
	DRAWLINE
	LOADGLOBAL
	CALL TROPHY
	RESULT = 2
	
	;PRINTBUTTON "[2] ゲームを終了する  ","" 
	;PRINTL 
	;DRAWLINE
	;PRINTFORMW %GAMEBASE_TITLE%を終了します
	;QUIT
ELSEIF RESULT == 9
	CLEARLINE 2
	DRAWLINE
	CALL RELEASE_NOTE
	RESULT = 9
ELSE
	PRINTL 
	GOTO REPEAT_LOOP
ENDIF
RETURN RESULT

$SKIP_RESTART
CALL SYSTEM_TITLE

