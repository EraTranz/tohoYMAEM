;YM→YMAEコンバータによって作成されました
;================================================
;COMFを参照して、コマンド実行可能までの条件数を調べる
;■戻り値（★注意：式中関数ではないのでRESULT必須）
;	負値：実行不能、拒否される
;	   0：実行可能か、そもそも常時実行可能
;	正値：条件を満たした上で実行可能
;------------------------------------------------
;■引数
;ARG
;	COM番号。省略時はSELECTCOM
;================================================
@K2008_ASK_COMORDER(ARG = __INT_MIN__)
;今の判定値を念のため退避させる
;TFLAG:240 = 基準値
;TFLAG:241 = 実行値
#DIM _TFLAG240
#DIM _TFLAG241
#DIM _DIST
_TFLAG240 = TFLAG:240
_TFLAG241 = TFLAG:241

;引数が負値なら、SELECTCOMを入れる
SIF ARG < 0
	ARG = SELECTCOM

;COMFで条件を調べる
CALL COM_ORDER, ARG, 1

;戻り値を準備して、TFLAGを戻す
_DIST = TFLAG:241 - TFLAG:240
TFLAG:240 = _TFLAG240
TFLAG:241 = _TFLAG241

;実行値-現在値を返す
RETURN _DIST

