;YM→YMAEコンバータによって作成されました
;-------------------------------------------------
;口に射精
;-------------------------------------------------
@K2008_EJAC_GENERAL_15
;イラマチオのみ別処理（喉奥射精のため）
IF SELECTCOM == 230
	CALL K2008_VOICE("喉奥射精")
ELSE
	CALL K2008_VOICE("口内射精")
ENDIF

;精飲絶頂している場合は呼び出す（通常の絶頂口上では呼び出さないようになっている）
IF NOWEX:精飲 > 0
	TRYCALLFORM K2008_RANK{K2008F_RANK()}_SP_ORGASM_GENERAL_13
ENDIF
