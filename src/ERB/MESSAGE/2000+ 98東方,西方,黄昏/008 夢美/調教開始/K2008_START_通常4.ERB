;YM→YMAEコンバータによって作成されました
;=================================================
;RANK = 4
;実際のところ、RANK4ってあんまり期間ないんですよね……　ほぼ通過点
;=================================================

;-------------------------------------------------
;前回調教からの経過時間で分岐
;RANK5を呼ぶ（とりあえず）
;-------------------------------------------------
@K2008_RANK4_START1(ARGS = "")
CALL K2008_RANK5_START1(ARGS)
RETURN RESULT

;-------------------------------------------------
;Ｃおねだり
;RANK5を呼ぶ（とりあえず）
;-------------------------------------------------
@K2008_RANK4_START2(ARGS = "")
CALL K2008_RANK5_START2(ARGS)
RETURN RESULT

;-------------------------------------------------
;Ｖおねだり
;RANK5を呼ぶ（とりあえず）
;-------------------------------------------------
@K2008_RANK4_START3(ARGS = "")
CALL K2008_RANK5_START3(ARGS)
RETURN RESULT

;-------------------------------------------------
;Ａおねだり
;RANK5を呼ぶ（とりあえず）
;-------------------------------------------------
@K2008_RANK4_START4(ARGS = "")
CALL K2008_RANK5_START4(ARGS)
RETURN RESULT

;-------------------------------------------------
;Ｂおねだり
;RANK5を呼ぶ（とりあえず）
;-------------------------------------------------
@K2008_RANK4_START5(ARGS = "")
CALL K2008_RANK5_START5(ARGS)
RETURN RESULT

;-------------------------------------------------
;脱衣挑発
;RANK5を呼ぶ（とりあえず）
;-------------------------------------------------
@K2008_RANK4_START10(ARGS = "")
CALL K2008_RANK5_START10(ARGS)
RETURN RESULT
