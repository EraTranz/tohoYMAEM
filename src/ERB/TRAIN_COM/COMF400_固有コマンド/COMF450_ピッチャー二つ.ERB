;=============================================================================
;ピッチャー二つ
;=============================================================================
;お酒を飲ませるの丸パクリでございます
;ビールを一気に2つ呑ませるコマンド。効果としてはビールによるイきつけ三杯と同じ
;1本節約できるのがウリ。それより問題なのは絶頂回数に関わらず実行できるあたりなのかも
;--------------------------------------------------
;実行判定
;--------------------------------------------------
@COM_ABLE450
;個別設定コマンドフィルタ
SIF DITEMTYPE:999:450 > 0
	RETURN 0
;調教者奉仕・ウフフ系コマンドを有効にしてないとダメ
SIF (FLAG:15 & 2048) == 0
	RETURN 0
;キャラ専用コマンドを有効にしてないとダメ
SIF (FLAG:15 & 16384) == 0
	RETURN 0
;調教者が天治でないとダメ
SIF NO:PLAYER != 2042
	RETURN 0
;ビール2つ持っていないとダメ
SIF ITEM:ビール < 2
	RETURN 0
;主人に対する好感度が100以上必要
SIF CFLAG:好感度 < 100
	RETURN 0
;反発刻印が0でないとダメ
SIF MARK:反発刻印
	RETURN 0
;睡眠中は不可
SIF EQUIP:睡眠薬
	RETURN 0
;猿轡付けていると無理
SIF TEQUIP:口装着器具
	RETURN 0
;シャワー中はダメ
SIF TEQUIP:シャワープレイ
	RETURN 0
;○○風呂入浴中はダメ
SIF TEQUIP:特殊風呂入浴中
	RETURN 0
;オムツプレイ中はダメ
SIF TEQUIP:オムツプレイ
	RETURN 0
;触手調教中はダメ
SIF TEQUIP:触手調教
	RETURN 0
;失神中は不可
SIF TFLAG:899 > 0
	RETURN 0
;距離のチェック
SIF EQUIP:戦闘中 && COM_ABLE_BATTLE(TARGET, PLAYER, 450) == 0
	RETURN 0
RETURN 1

;--------------------------------------------------
;コマンド本体
;--------------------------------------------------
@COM450
PRINTL 儀式「とりあえずピッチャー二つで」

;戦闘判定
CALL COM_TRY_BATTLE, 450
;実行できない
SIF RESULT == 1
	RETURN 1

TSTR:0 = 儀式「とりあえずピッチャー二つで」
CALL DISPLAY_KOJO_TRAIN_MESSAGE_F_COM
;-------------------------------------------------
;ソースの計算
;-------------------------------------------------
DOWNBASE:0 += 60
DOWNBASE:1 += 60
SOURCE:酒酔い = 1600

;ビールを2つ消費
ITEM:ビール -= 2

;このコマンドを実行したことを記録
CFLAG:160 |= 1
RETURN 1

;--------------------------------------------------
;調教時メッセージ
;--------------------------------------------------
@SYSTXT_TRAIN_MESSAGE_F_COM_450
PRINTFORML %CALLNAME:TARGET%にピッチャー2杯のビールを呑ませた
PRINTFORML さっぱりした喉越しで%CALLNAME:TARGET%の気分は少し持ちあがった
